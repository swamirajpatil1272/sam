on:
  schedule:
  - cron:  "* * * * *"
  workflow_dispatch:

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: repo-sync
      uses: repo-sync/github-sync@v2
      with:
        source_repo: "swamirajpatil1272/sam2"
        source_branch: "main"
        destination_branch: "new"
        github_token: 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
